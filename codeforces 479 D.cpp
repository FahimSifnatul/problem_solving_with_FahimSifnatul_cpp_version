#include<bits/stdc++.h>
using namespace std;
#define ll long long int
map<ll,bool>check;
map<ll,ll>node;

int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  int n;
  cin>>n;
  ll a[n];
  for(int i=0;i<n;i++)
  {
      cin>>a[i];
      check[a[i]]=true;
  }
  for(int i=0;i<n;i++)
  {
      ;
  }
}
